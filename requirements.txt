requests
pymupdf
python-docx

# Used by legacy scripts (vault_lint, find_broken_links, migrate)
rich
PyYAML

# Optional: for full tier
# yt-dlp          # video extraction
# openai-whisper  # audio transcription
